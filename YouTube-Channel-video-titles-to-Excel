Copy all YouTube Channel video titles to Excel
This is the code to scroll all the way to end of channel:
var scroll = setInterval(function(){ window.scrollBy(0, 1000)}, 1000);

This is the code to copy YouTube Video Titles and Links
window.clearInterval(scroll); console.clear(); urls = $$('a'); urls.forEach(function(v,i,a){if (v.id=="video-title-link"){console.log('\t'+v.title+'\t'+v.href+'\t')}});
